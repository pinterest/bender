language: go
go:
    - 1.7
before_install:
    - go get github.com/golang/lint/golint
install:
    - go get -d -v ./... && go build -v ./...
script:
    - go vet -x ./...
    - $HOME/gopath/bin/golint ./...
    - go test -v ./...
